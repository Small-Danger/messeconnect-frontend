VITE_API_URL=/api

# Google OAuth — même Client ID que GOOGLE_CLIENT_ID côté backend
VITE_GOOGLE_CLIENT_ID=

# true = données mockées (démo sans backend)
# false = API Laravel réelle (défaut)
VITE_USE_MOCK=false
