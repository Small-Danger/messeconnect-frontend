# Node 20.18 (nixpkgs nodejs_20) est trop ancien pour Vite 8 / Rolldown (^20.19.0).
[variables]
NODE_VERSION = "22"

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx --yes serve dist -s -l ${PORT:-3000}"
